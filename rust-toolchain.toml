# Rust toolchain configuration for Minuet
#
# Following Industrial Algebra ecosystem conventions:
# nightly channel + rustfmt + clippy components

[toolchain]
channel = "nightly"
components = ["rustfmt", "clippy"]
